name: atendimentos_android
description: Aplicativo Flutter de atendimentos (CRUD) focado em Android.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  sqflite: ^2.3.0
  path: ^1.9.0
  path_provider: ^2.1.0

  image_picker: ^1.1.1

  get_it: ^7.7.0
  injectable: ^2.5.0

  flutter_bloc: ^8.1.3
  equatable: ^2.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
